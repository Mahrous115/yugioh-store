# Runtime dependencies — fully pinned for reproducible container builds.
#
# Generated with `pip freeze` from a clean venv built from the previously
# unpinned requirements, on 2026-08-05, Python 3.12.
#
# Transitive dependencies are pinned too, not just direct ones. That is the point:
# an unpinned transitive can change under you between two builds of the same
# Dockerfile. Before this, starlette resolved to 1.3.1 and 1.4.0 on the same day.
#
# TO REGENERATE (ideally inside the Linux image — see the platform note):
#   pip install fastapi 'uvicorn[standard]' supabase python-dotenv pydantic slowapi
#   pip freeze --exclude pip --exclude setuptools --exclude wheel > requirements.txt
#
# ─── PLATFORM NOTE — read before your first image build ─────────────────────
#
# These pins were generated on Windows; your image is Linux. One package differs:
#
#   uvloop is part of uvicorn[standard] but only on non-Windows platforms
#   (uvicorn declares it as `sys_platform != 'win32' and ... != 'PyPy'`), so a
#   Windows freeze cannot see it and would silently drop it from the image.
#   Without uvloop, uvicorn falls back to the stdlib asyncio loop — the app
#   still works, just slower.
#
# It is therefore pinned below with the same marker uvicorn uses. Version 0.22.1
# ships cp312 manylinux wheels for x86_64 and aarch64 and satisfies uvicorn's
# `uvloop>=0.15.1`, but it is the ONE pin here that could not be installed and
# verified locally. Confirm it resolves on your first build.
#
# colorama is the mirror image: pulled in by click on Windows only. Harmless on
# Linux (pure Python) but unnecessary — drop it if you regenerate on Linux.

# ─── Direct dependencies ────────────────────────────────────────────────────
fastapi==0.141.1
uvicorn==0.52.1
supabase==2.31.0
python-dotenv==1.2.2
pydantic==2.13.4
slowapi==0.1.10

# uvicorn[standard] extras, pinned explicitly rather than resolved via the extra
httptools==0.8.0
PyYAML==6.0.3
watchfiles==1.2.0
websockets==15.0.1
uvloop==0.22.1 ; sys_platform != 'win32' and sys_platform != 'cygwin' and platform_python_implementation != 'PyPy'

# ─── Transitive dependencies ────────────────────────────────────────────────
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
certifi==2026.7.22
cffi==2.1.1
click==8.4.2
colorama==0.4.6
cryptography==50.0.0
Deprecated==1.3.1
deprecation==2.1.0
h11==0.16.0
h2==4.4.1
hpack==4.2.0
httpcore==1.0.9
httpx==0.28.1
hyperframe==6.1.0
idna==3.18
limits==5.8.0
multidict==6.7.1
packaging==26.3
postgrest==2.31.0
propcache==0.5.2
pycparser==3.0
pydantic_core==2.46.4
PyJWT==2.13.0
realtime==2.31.0
starlette==1.4.0
storage3==2.31.0
StrEnum==0.4.15
supabase-auth==2.31.0
supabase-functions==2.31.0
typing-inspection==0.4.2
typing_extensions==4.16.0
wrapt==2.3.0
yarl==1.24.5
